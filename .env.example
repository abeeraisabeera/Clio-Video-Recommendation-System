# Flask API (local: 5000, Hugging Face: 7860)
PORT=5000

# Next.js frontend — point at Flask (no Node proxy)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Hugging Face Space — same origin (set at Docker build time)
# NEXT_PUBLIC_API_URL=
